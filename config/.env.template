# PostgreSQL Configuration
# Note: For production, use Windows Credential Manager instead of storing passwords here
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=seo_audits
POSTGRES_USER=seo_etl_user
POSTGRES_PASSWORD=your_secure_password_here

# AWS Configuration
# Note: Use 'aws configure --profile s3_backup_user' instead of storing credentials here
AWS_PROFILE=s3_backup_user
AWS_REGION=us-east-1
S3_BUCKET=rankdre-sf-audits
